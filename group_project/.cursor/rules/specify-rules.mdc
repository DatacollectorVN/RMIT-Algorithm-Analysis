---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# group_project Development Guidelines

Auto-generated from feature plans `001-similarity-search-topk`, `002-two-phase-cli`. Last updated: 2026-04-06

## Active Technologies

- Python **3.12+**, **stdlib only** (no PyPI) — see `.specify/memory/constitution.md`
- Features: top-k similarity search (`specs/001-similarity-search-topk/`); CLI subcommands `generate-corpus` / `search` (`specs/002-two-phase-cli/`)
- I/O: UTF-8 JSON corpus/query on disk; `generate-corpus` prints file paths; `search` prints JSON results (`.rmit/corpus/` is gitignored)

## Project Structure

```text
group_project/
├── src/
│   ├── main.py              # CLI entry
│   └── services/
│       ├── helper.py        # Pure utils (min-max, bbox, weighted dist to AABB)
│       ├── core/            # exceptions
│       ├── similarity/      # pipeline / profiles
│       ├── search/          # distance, topk, benchmark, strategies/
│       └── io/              # jsonio
└── tests/
```

## Commands

```bash
cd group_project
export PYTHONPATH="${PYTHONPATH}:$(pwd)/src"
python -m unittest discover -s tests -p 'test_*.py'
python src/main.py --help
python src/main.py generate-corpus --help
python src/main.py search --help
# generate-corpus writes .rmit/corpus/<timestamp>/corpus.json (+ metadata); prints paths
# Optional: uv run python src/main.py generate-corpus --N 100 --seed 1
# Optional: uv run python src/main.py search --corpus .rmit/corpus/.../corpus.json --query query.json --strategy baseline
```

## Code Style

- PEP 8, strict type hints, Google-style docstrings, custom exception hierarchy
- Tests: `unittest` only (no pytest)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->

## Recent Changes

- 002-two-phase-cli: **subcommands** `generate-corpus` (`--N`, `--seed`) and `search` (`--corpus`, `--query`, `--strategy`, `--benchmark`); `get_synthetic_corpus` / `get_synthetic_query` on search path; optional `uv run` examples in docs
